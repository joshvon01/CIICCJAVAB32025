package VCashAppArray;

/**
 * Handles checking a user's balance.
 */
public class CheckBalance {
    public static void checkBalance(User user) {
        System.out.println("Your current balance is: " + user.getBalance());
    }
}
